package transport;

public interface Animal {
    void move();
    void eat(int i);
    String speak();
}

//an interface provides a standard set of methods that it's children have/can use
//every child can move eat speack
